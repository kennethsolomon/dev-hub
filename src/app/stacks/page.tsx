import { AppShell } from '@/components/layout/app-shell';
import { StacksPageContent } from '@/components/stacks/stacks-page';

export default function StacksPage() {
  return (
    <AppShell>
      <StacksPageContent />
    </AppShell>
  );
}
