import { AppShell } from '@/components/layout/app-shell';
import { UpdatesPageContent } from '@/components/updates/updates-page';

export default function UpdatesPage() {
  return (
    <AppShell>
      <UpdatesPageContent />
    </AppShell>
  );
}
